interface NavLinkType {
  label: string;
  link: string;
  color: string;
}

type ParaphrasingModeType =
  | 'standard'
  | 'fluent'
  | 'formal'
  | 'simple'
  | 'creative'
  | 'expanded'
  | 'shorten';
